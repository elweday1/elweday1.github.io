---
import { type Show } from "@utils/shows";
import Image from "astro/components/Image.astro";
interface Props {
  showsData: Show[];
}

const { showsData } = Astro.props;
---

<div class="slider-container">
  <nav class="slider-nav">
    {
      showsData.map(({ imdbID }) => (
        <a href={`#slide-${imdbID}`} class="nav-dot" />
      ))
    }
  </nav>

  <div class="slides">
    {
      showsData.map(({ imdbID, Poster, Title }) => (
        <div id={`slide-${imdbID}`} class="slide-item group">
          <Image
            src={Poster}
            width={150}
            height={225}
            class="slide-image"
            alt={Title}
          />
          <a
            href={`https://www.imdb.com/title/${imdbID}`}
            class="slide-overlay"
          >
            <span class="slide-title">{Title}</span>
          </a>
        </div>
      ))
    }
  </div>
</div>

<style>
  .slider-container {
    width: 100%;
    display: flex;
    flex-direction: column;
    align-items: stretch;
    gap: 0.75rem;
  }

  /* Navigation Dots */
  .slider-nav {
    display: flex;
    gap: 0.5rem;
    justify-content: flex-start;
  }

  .nav-dot {
    width: 8px;
    height: 8px;
    border: 1px solid #63ddf5;
    background: #061b24;
    border-radius: 2px;
    transition:
      background 0.3s,
      transform 0.2s;
  }

  .nav-dot:hover,
  .nav-dot:focus {
    background: #63ddf5;
    transform: scale(1.2);
  }

  /* Slides Wrapper */
  .slides {
    display: flex;
    width: 100%;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    scroll-behavior: smooth;
    gap: 0.75rem;
    padding-bottom: 10px;

    /* Hide Scrollbar */
    -ms-overflow-style: none; /* IE/Edge */
    scrollbar-width: none; /* Firefox */
  }

  .slides::-webkit-scrollbar {
    display: none; /* Chrome/Safari */
  }

  /* Individual Slide */
  .slide-item {
    flex: 0 0 auto;
    width: 132px;
    aspect-ratio: 2/3;
    position: relative;
    scroll-snap-align: center;
    border: 1px solid rgba(9, 44, 46, 0.18);
    border-radius: 4px;
    overflow: hidden;
    background: #f0f0f0;
  }

  .slide-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  /* Overlay Logic */
  .slide-overlay {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(rgba(157, 242, 255, 0.08) 50%, rgba(0, 0, 0, 0.08) 50%),
      linear-gradient(to top, rgba(0, 0, 0, 0.86), transparent);
    background-size: 100% 5px, auto;
    display: flex;
    align-items: flex-end;
    padding: 0.75rem;
    opacity: 0;
    transition: opacity 0.3s ease;
    text-decoration: none;
  }

  .slide-item:hover .slide-overlay {
    opacity: 1;
  }

  .slide-title {
    color: white;
    font-size: 0.75rem;
    font-weight: 900;
    line-height: 1.2;
  }
</style>
